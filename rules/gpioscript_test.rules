rule "GPIO test 19"
when
    Item GPIO_Test received command
then
    logDebug("GPIO_test", "GPIO_Test received "+receivedCommand)
    val scriptItem = GPIOScript_Args
    val cmd = "19 "+receivedCommand
    scriptItem.sendCommand(cmd)
    logDebug("GPIO_test", "Sent to the script channel "+cmd)
end